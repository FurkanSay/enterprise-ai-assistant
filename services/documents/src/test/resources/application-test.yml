spring:
  datasource:
    url: jdbc:tc:postgresql:16:///kai_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    enabled: true
  data:
    redis:
      url: redis://localhost:6379

minio:
  endpoint: http://localhost:9000
  access-key: test
  secret-key: testtesttest
  bucket:
    documents: test-documents
